(() => {
  const storageKey = 'dailyBusinessReportForm'; const form = document.querySelector('#reportForm'); const preview = document.querySelector('#reportPreview'); const message = document.querySelector('#actionMessage'); const saveStatus = document.querySelector('#saveStatus'); const fields = [...document.querySelectorAll('[data-field]')]; document.querySelectorAll('input[type="number"]').forEach((input) => { input.inputMode = 'text'; });
  const number = (field) => { const value = Number(document.querySelector(`[data-field="${field}"]`).value); return Number.isFinite(value) ? value : 0; }; const text = (field) => document.querySelector(`[data-field="${field}"]`).value.trim(); const display = (value) => Number.isInteger(value) ? String(value) : String(Math.round((value + Number.EPSILON) * 100) / 100); const quarterTitle = () => `${['一', '二', '三', '四'][Math.floor(new Date().getMonth() / 3)]}季度情况每日汇报`;
  function reportText() { const deposit = number('companyDeposit') + number('personalDeposit'); const volume = number('companyVolume') + number('personalVolume'); const inclusiveNewTotal = number('inclusiveNewCompany') + number('inclusiveNewPersonal'); const inclusiveCustomersTotal = number('inclusiveCustomersCompany') + number('inclusiveCustomersPersonal'); return [quarterTitle(),`1.存款：当日${display(deposit)}万（公司${display(number('companyDeposit'))}万，个人${display(number('personalDeposit'))}万）`,`2.全量：当日${display(volume)}万（公司${display(number('companyVolume'))}万，个人${display(number('personalVolume'))}万）。`,`3.贷款：当日非房${display(number('nonMortgageAmount'))}万，收件${display(number('nonMortgageCases'))}笔，金额${display(number('nonMortgageCaseAmount'))}万，批额${display(number('approvedHouseholds'))}户；房贷${display(number('mortgageAmount'))}万，收件${display(number('mortgageCases'))}笔；公司贷款${display(number('corporateLoanAmount'))}万。`,`4.普惠：当日新增${display(inclusiveNewTotal)}万（公司${display(number('inclusiveNewCompany'))}万，个人${display(number('inclusiveNewPersonal'))}万）；客户数${display(inclusiveCustomersTotal)}户（公司${display(number('inclusiveCustomersCompany'))}户，个人${display(number('inclusiveCustomersPersonal'))}户）；收件${display(number('inclusiveCases'))}笔，金额${display(number('inclusiveCaseAmount'))}万。`,`5.代发薪：${text('payroll') || '0/0'}`,`6.中收及客群：五大投资类（理财${display(number('wealthManagement'))}万，私行代销${display(number('privateBankSales'))}万，基金${display(number('fund'))}万，保险趸交${display(number('insurance'))}万、贵金属${display(number('preciousMetals'))}万），信用卡${display(number('creditCard'))}户，三方${display(number('thirdParty'))}户，一类卡${display(number('firstClassCard'))}张`,`7.走访情况：${text('visits') || '无'}`].join('\n'); }
  function save() { localStorage.setItem(storageKey, JSON.stringify(Object.fromEntries(fields.map((input) => [input.dataset.field, input.value])))); saveStatus.textContent = '已自动保存'; } function refresh() { document.querySelector('#depositTotal').textContent = display(number('companyDeposit') + number('personalDeposit')); document.querySelector('#volumeTotal').textContent = display(number('companyVolume') + number('personalVolume')); document.querySelector('#inclusiveNewTotal').value = display(number('inclusiveNewCompany') + number('inclusiveNewPersonal')); document.querySelector('#inclusiveCustomersTotal').value = display(number('inclusiveCustomersCompany') + number('inclusiveCustomersPersonal')); preview.textContent = reportText(); } function notify(content) { message.textContent = content; setTimeout(() => { if (message.textContent === content) message.textContent = ''; }, 2500); } function restore() { try { const saved = JSON.parse(localStorage.getItem(storageKey)); if (saved) fields.forEach((input) => { if (saved[input.dataset.field] !== undefined) input.value = saved[input.dataset.field]; }); } catch (_) { localStorage.removeItem(storageKey); } }
  form.addEventListener('input', () => { saveStatus.textContent = '保存中…'; refresh(); save(); }); document.querySelector('#generateButton').addEventListener('click', () => { refresh(); notify('日报已生成'); }); document.querySelector('#copyButton').addEventListener('click', async () => { try { await navigator.clipboard.writeText(reportText()); } catch (_) { const area = document.createElement('textarea'); area.value = reportText(); document.body.append(area); area.select(); document.execCommand('copy'); area.remove(); } notify('日报已复制，可直接粘贴到微信'); }); document.querySelector('#exportButton').addEventListener('click', () => { const blob = new Blob([reportText()], { type: 'text/plain;charset=utf-8' }); const link = Object.assign(document.createElement('a'), { href: URL.createObjectURL(blob), download: '每日汇报.txt' }); link.click(); URL.revokeObjectURL(link.href); notify('已导出每日汇报.txt'); }); document.querySelector('#newDayButton').addEventListener('click', () => { fields.forEach((input) => { input.value = input.type === 'number' ? '0' : (input.dataset.field === 'payroll' ? '0/0' : ''); }); refresh(); save(); notify('已开启新一天，已清空全部数据'); }); restore(); refresh();
})();
